package mai.testing.platform.handlers.tests;

import mai.testing.platform.handlers.BaseHandler;

public class Tests_handler implements BaseHandler {
    @Override
    public String[] handel(String line) {
        return new String[0]; //TODO: написать обработчик
    }
}
